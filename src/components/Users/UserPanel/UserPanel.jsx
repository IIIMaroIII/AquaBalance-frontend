import css from './UserPanel.module.css';
import UserBar from './UserBar/UserBar.jsx';

const UserPanel = () => {
  return (
    <div className={''}>
      <UserBar />
    </div>
  );
};

export default UserPanel;
